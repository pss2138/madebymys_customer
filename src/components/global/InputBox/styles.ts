import {StyleSheet} from 'react-native';

// Styles
const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  textInputArea: {
    flex: 1,
    alignContent: 'center',
    marginRight: 50,
  },
  button: {},
});

export default styles;
