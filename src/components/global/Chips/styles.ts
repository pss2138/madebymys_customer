import {StyleSheet} from 'react-native';

// Styles
const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    borderRadius: 16,
    paddingLeft: 6,
    paddingRight: 8,
    paddingVertical: 4,
  },
  text: {
    marginLeft: 4,
  },
});

export default styles;
